def line (deli)
    if deli.empty?
        puts "there is nobody in line ".
    else
        current_line = "there is nobody in line ".
        deli.each.with.index do| person |
            current_line 
        end
        def take_a_number  (deli , name )
            deli << name
